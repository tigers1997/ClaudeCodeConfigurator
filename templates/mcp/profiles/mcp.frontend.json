{
  "//": "Profile: frontend. Playwright for browser/UI work. Use during regression checks, visual verification, or end-to-end debugging. Runs via: ./claude-ctx frontend",
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@0.0.70"]
    }
  }
}
